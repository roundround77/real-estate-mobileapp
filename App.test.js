import React from 'react';
import renderer from 'react-test-renderer';

import App from './App';

describe('testing App.js', () => {
    test('Node Modules : //', () => {
      expect(0).toBe(0);
    });
});
